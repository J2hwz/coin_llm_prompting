# Instructions

You are controlling an agent in a grid-based environment with full observability. The agent can move in four directions: up, down, left, and right. The environment contains walls, open spaces, a goal location, and two coins. The following symbols are used in the grid representation:

Legend:
---------------
#: Wall
_: Open Space (can be visited)
G: Goal
A: Current agent position
C: Coin (collectible item)
----------------

Your objective is to navigate from the current position (A) to the goal (G), collecting exactly ONE of the two coins (C) along the way. You should only step onto ONE coin cell before reaching the goal, you do not need to collect both — collecting one is sufficient. After collecting one coin, proceed directly to the goal.

You will receive the current state of the grid as a NxM matrix of symbols separated by whitespaces, and with coordinates for each row and column. For example, given the 9x5 grid:

  0 1 2 3 4 5 6 7 8
0 # # # # # # # # #
1 # _ _ _ _ _ _ _ #
2 # A # # # # # G #
3 # _ C _ _ _ C _ #
4 # # # # # # # # #

The goal is in position (7,2), the agent's position A is (1,2), and the two coins C are at (2,3) and (6,3). Collect one coin, then reach G.

The agent's possible actions at each step are:

Actions:
- UP: Move Up
- DOWN: Move Down
- LEFT: Move Left
- RIGHT: Move Right

Your final answer should be a valid JSON object provided in the following form:

```
{
  "action": "<UP|DOWN|LEFT|RIGHT>"
}
```

DO NOT INCLUDE ANY `json` or `jsonb` in your response NOR TICK MARKS LIKE THIS: ```json ```. Start with { and end with } exactly.

{% if history %}
History of previous moves:
{% for position, action, coins_collected in history -%}
({{ position[0] }}, {{ position[1] }}): {{ action }}{% if coins_collected is not none %}, coins collected: {{ coins_collected }}{% endif %}
{% endfor %}

{% endif %}
# Inputs

Current grid state:

{{grid_state}}
